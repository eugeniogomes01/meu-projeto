let pessoa = {
    nome: "Eugênio",
    idade: 22,
    altura: 1.85
};
let compras = ["pão", "presunto", "queijo"];

let json = JSON.stringify(compras);

console.log(json);

//let reObejeto = JSON.parse(json);

//console.log(reObejeto)
